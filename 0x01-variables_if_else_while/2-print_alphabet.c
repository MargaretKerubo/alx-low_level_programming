#include <stdio.h>
/**main - entry point
 * Description: print the alphabet in lower case
 * Return 0 on success
 */
int main()
{
	int l = 'a';
		for (l = 'a'; l <= 'z'; l++);
		{
			putchar(l);
		}
	return (0);
}
